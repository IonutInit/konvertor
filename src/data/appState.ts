import { OptionsStateType } from "../../types";

const appState = {
  konvertor: true,
  extendedList: false,
  measureType: "",
  metric: true,
  fromUnit: "m",
  toUnit: "ft",
  decimals: 2,
  optionsState: "" as OptionsStateType,
};

export default appState;
