import { OptionsStateType } from "../../types";

const appState = {
  konvertor: true,
  extendedList: false,
  measureType: "",
  formerMeasureType: "",
  metric: true,
  fromUnit: ["m"],
  fromValue: [1],
  toUnit: "ft",
  decimals: 2,
  optionsState: "" as OptionsStateType,
};

export default appState;
